# Quick Task 260416-19h: concluir policy de hooks reativos na camada DamageEvent

**Date:** 2026-04-16
**Status:** Completed

## Goal

Garantir que a pipeline de dano use a policy fixada em `DamageEvent` (`allowOnDot` / `allowOnNestedDamage`) sem duplicar estado em `combatEvents`.

## Tasks

### 1. Manter `combatEvents` stateless

- Files: `shared/engine/combat/combatEvents.js`
- Action: adicionar apenas um ponto de extensão opcional (`shouldRunHook`) no emissor, sem reintroduzir `DEFAULT_DAMAGE_HOOK_POLICY` local
- Verify: não há estado de policy duplicado em `combatEvents`; filtro depende de callback externo
- Done: camada de evento continua genérica e sem acoplamento ao estado do `DamageEvent`

### 2. Fazer a step de before hooks respeitar policy do DamageEvent

- Files: `shared/engine/combat/pipeline/04_beforeHooks.js`
- Action: passar callback para `emitCombatEvent` delegando decisão para `event.shouldRunDamageReactiveHook(...)`
- Verify: eventos `onBeforeDmgDealing`/`onBeforeDmgTaking` passam pelo gate da instância
- Done: before-hooks aplica exatamente a policy fixada na classe `DamageEvent`

### 3. Fazer a step de after hooks respeitar policy do DamageEvent

- Files: `shared/engine/combat/pipeline/07_afterHooks.js`
- Action: mesmo padrão de delegação no `_processHook` de after
- Verify: `onAfterDmgDealing`/`onAfterDmgTaking` passam pelo gate da instância
- Done: after-hooks também obedece a policy centralizada

## Outcome

- Policy de hooks reativos permanece centralizada em `DamageEvent`.
- Pipeline `04/07` utiliza explicitamente essa policy via callback.
- `combatEvents` voltou ao papel de dispatcher genérico, sem estado duplicado.
