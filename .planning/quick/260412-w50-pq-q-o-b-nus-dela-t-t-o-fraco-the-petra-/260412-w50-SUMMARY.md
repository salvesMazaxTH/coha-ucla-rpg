# Quick Task 260412-w50 Summary

## Completed

- Criado `scripts/damageEventLab.js` para executar `DamageEvent` em isolamento com entrada via CLI.
- Adicionado modo de comparacao (`--compare-passive`) para executar o mesmo ataque em dois cenarios: sem stacks e com stacks maximos da passiva.
- Exposto comando no `package.json` (`npm run damage-lab`).

## Validation

- `get_errors` em `scripts/damageEventLab.js` e `package.json`: sem erros.
- Execucao:
  - `npm run damage-lab -- --attacker theopetra --defender bruno --skill golpe_petreo --compare-passive`
- Resultado observado:
  - Sem stacks: 70 de dano aplicado.
  - Com stacks maximos: 126 de dano aplicado.
  - Razao observada: `1.800x`.

## Notes

- O projeto nao atende ao pre-requisito formal do `gsd-quick` (`ROADMAP.md` ausente), entao o registro foi feito em modo best-effort.
