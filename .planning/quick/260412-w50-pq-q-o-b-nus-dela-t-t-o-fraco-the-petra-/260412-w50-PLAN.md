# Quick Task 260412-w50 Plan

## Goal

Criar uma forma de testar `DamageEvent` em isolamento (sem iniciar partida com dois clientes) para reproduzir e validar o comportamento do bonus de dano da passiva da Theopetra.

## Tasks

1. Adicionar um script CLI para simular cenarios de `DamageEvent` com atacante/alvo/habilidade configuraveis.
2. Incluir modo de comparacao com e sem stacks da passiva para medir o multiplicador observado no dano final.
3. Expor comando no `package.json` e executar validacao local do caso Theopetra x Bruno.

## Notes

- O workflow `gsd-quick` nao pode seguir o gate formal porque o repositorio nao possui `ROADMAP.md` ativo.
- Execucao feita em modo best-effort, mantendo artefatos minimos em `.planning/quick/`.
